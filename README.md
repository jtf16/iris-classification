# Iris classification project

Simple project to test the accuracy of different models in Iris classification, based on some of the iris features.

## Installation

Install the needed libraries for the project to run:
```bash
pip3 install -r requirements.txt  
```

## Run

```bash
python3 irisML.py
```
